# 영단어 타이핑 게임 알고리즘

1. `pygame`, `random`, `time` 라이브러리를 불러온다.
2. 정답/오답 효과음(`good.wav`, `bad.wav`)을 준비한다.
3. `word.txt`의 단어들을 `words` 리스트에 저장한다.
4. 게임 시작 시 점수를 0으로 초기화하고 시작 시간을 기록한다.
5. 총 5번 반복하면서 `words`에서 랜덤 단어를 출제한다.
6. 사용자가 입력한 단어와 제시된 단어를 비교한다.
   - 정답: 점수 +1, 정답 효과음 재생
   - 오답: 오답 효과음 재생
7. 5문제가 끝나면 종료 시간을 기록하고 총 소요 시간을 계산한다.
8. 최종 점수가 3점 이상이면 합격, 미만이면 불합격을 출력한다.
9. 최종 점수와 게임 소요 시간을 함께 출력한다.